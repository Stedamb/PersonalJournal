import { Button } from "./ui/button";

const Header = () => {
    return (
        <div className="container-xl flex justify-center pt-20">
            <Button variant="default">Test</Button>
        </div>
    );
};

export default Header;
